import 'package:flutter/material.dart';

class CaravanRevenue extends StatelessWidget {
  final String title;
  const CaravanRevenue({super.key, required this.title});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('data'),
      ),
      body: Center(
        child: Text('data'),
      ),
    );
  }
}

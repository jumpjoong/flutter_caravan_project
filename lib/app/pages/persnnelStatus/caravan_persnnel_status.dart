import 'package:flutter/material.dart';

class CaravanPersnnelStatus extends StatelessWidget {
  final String title;
  const CaravanPersnnelStatus({super.key, required this.title});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('data'),
      ),
      body: Center(
        child: Text('data'),
      ),
    );
  }
}
